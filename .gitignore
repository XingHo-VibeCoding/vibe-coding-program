# ==========================================================
#  「每日一个AI概念详解」网站  .gitignore
#  作用：告诉 Git 哪些文件“永不提交、永不上传”
#  规则：写在此文件里的路径/文件名，Git 会完全忽略
# ==========================================================

# ---------- 1. 密钥与隐私：绝对禁止上传 ----------
# 环境变量文件（里面通常放着 API Key、数据库密码）
.env
.env.*
# 例外：示例文件可以上传（里面只有变量名，没有真实值）
# 注意：! 这行必须写在 .env / .env.* 之后，且不能带行尾注释
!.env.example

# 各类密钥 / 证书 / 凭据文件
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
*.ppk
id_rsa
id_rsa.pub
id_ed25519
*.crt
*.cer

# 常见“配置里有密码”的文件
secrets.json
secret.json
credentials.json
*credentials*.json
config.local.*
*.local

# 云服务凭据目录
.aws/
.azure/
.gcloud/
.netrc
.npmrc
.pypirc

# ---------- 2. 依赖目录：体积巨大，绝不提交 ----------
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/
env/
ENV/
env.bak/
venv.bak/
bower_components/
.pnp
.pnp.js
.yarn/cache
.yarn/install-state.gz

# ---------- 3. 本地数据库文件：含真实数据，不上传 ----------
*.sqlite
*.sqlite3
*.sqlite3-journal
*.db
*.db-journal
*.mdb
*.accdb
data/*.db
data/*.sqlite*
local.db
dev.db
*.dump
*.sql.gz

# ---------- 4. 构建产物 / 缓存 ----------
dist/
build/
out/
.next/
.nuxt/
.cache/
.parcel-cache/
coverage/
*.tsbuildinfo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.eslintcache

# ---------- 5. 日志 / 临时文件 ----------
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tmp
*.temp
*.bak
*.swp
*~
.DS_Store
Thumbs.db
desktop.ini

# ---------- 6. 编辑器 / IDE 本地设置 ----------
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ---------- 7. AI 工具的本地缓存与本地笔记（含项目记忆，不上传） ----------
.workbuddy/

# ---------- 8. 其他本地/私密内容 ----------
# 草稿、私人笔记、未发布的素材
private/
draft/
*.private.*

# 本地 Git 操作手册（含本机绝对路径，属于个人工作文档，不属于网站内容）
GitHub上传清单.md

# 本地学习记录 / 证据文件（不上传，避免仓库里混入非网站内容）
Day*-本地Git终端记录.txt
Day2-本地Git终端记录.txt
